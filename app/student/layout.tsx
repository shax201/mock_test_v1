// Public wrapper for student routes. Do not gate here; protected pages live under (protected)/.

export default function StudentPublicLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
